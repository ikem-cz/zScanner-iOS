platform :ios, '12.0'

inhibit_all_warnings!

def common_pods
    # pod 'Alamofire'         # Networking library
    # pod 'SwiftyJSON'        # Parsing JSON the swift style
    pod 'SnapKit'           # Easy constraints handling
    pod 'Firebase/Core'     # Don't ask me
    pod 'Firebase/Database'
end

target 'zScanner' do
    use_frameworks!
    common_pods
end
